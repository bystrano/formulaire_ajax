(function ($) {
    $.fn.formulairesAjax = function (options) {

        var config = $.extend({
            // These are the defaults.
            // color: "#556b2f",
            // backgroundColor: "white"
        }, options);

        return this;
    };
}(jQuery));
